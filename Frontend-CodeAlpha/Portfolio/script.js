// Navbar scroll effect animation or interactive features can go here
document.addEventListener('DOMContentLoaded', () => {
    console.log("Portfolio loaded successfully!");
    
    // Example: Optional simple animation when clicking navigation links
    const navLinks = document.querySelectorAll('.nav-links a');
    
    navLinks.forEach(link => {
        link.addEventListener('click', () => {
            // Can log tracking or trigger custom interactions if needed
        });
    });
});